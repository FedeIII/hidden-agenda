# Build output, committed for GitHub Pages and the VPS. Never hand-format it — the next build
# would undo it, and format:check has been red on the server bundle ever since it landed.
docs/
dist-server/

node_modules/
package-lock.json

# Playwright artefacts
test-results/
playwright-report/
blob-report/

# Prose. Reflowing it would bury real edits in whitespace churn.
*.md
